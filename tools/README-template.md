<!--
  Шаблон README для папки варіанту ДЗ (напр. "dz1.4 (LED + BUTTON)/modes").

  1. Скопіюй:    cp tools/README-template.md "dzX.Y (ТЕМА)/варіант/README.md"
  2. Заповни всі плейсхолдери в подвійних фігурних дужках.
     Перевірка, що нічого не лишилось:  grep -n '{{' "dzX.Y (ТЕМА)/варіант/README.md"
  3. Видали зайві рядки та розділи і цей коментар.

  Файли, які очікує шаблон: main.cpp, diagram.json, wokwi.toml, schema.png, photo.jpeg, video.MOV.
  demo.gif і video.mp4 генерує:  tools/media.sh "dzX.Y (ТЕМА)/варіант"
-->
# ДЗ {{НОМЕР, напр. 1.4}} — {{НАЗВА ВАРІАНТУ}} ({{ТЕМА, напр. LED + BUTTON}})

{{Одне-два речення: що робить прошивка}} на **ESP32-S3-DevKitC-1**.

| {{Вхід, напр. Кнопка}} | {{Вихід / режим}} | Поведінка |
|---|---|---|
| {{🔵 Синя (GPIO 7)}} | {{🔵 Синій LED (GPIO 16)}} | {{що відбувається}} |
| {{🔴 Червона (GPIO 15)}} | {{🔴 Червоний LED (GPIO 3)}} | {{що відбувається}} |

## Демо

<a href="video.mp4"><img src="demo.gif" width="420" alt="Демо: {{що видно на відео}}"></a>

▶️ [Відео в повній якості (MP4)](video.mp4) · [оригінал (MOV)](video.MOV)

## Схема та фото

| Схема (Wokwi) | Зібрана плата |
|---|---|
| <img src="schema.png" width="400" alt="Схема підключення у Wokwi"> | <img src="photo.jpeg" width="400" alt="Фото зібраної схеми на макетній платі"> |

### Підключення

| Компонент | Пін ESP32-S3 | Примітка |
|---|---|---|
| {{Синя кнопка}} | GPIO {{7}} | {{другий контакт → GND, `INPUT_PULLUP`}} |
| {{Синій LED}} | GPIO {{16}} | {{через резистор 220 Ω, катод → GND}} |

{{Особливості схеми, напр. «Кнопки активні низьким рівнем: натиснута кнопка замикає пін на GND.»}}

## Як працює код

Файл: [`main.cpp`](main.cpp)

- `{{Клас або функція}}` — {{за що відповідає}}.
- `{{Клас або функція}}` — {{за що відповідає}}.
- `loop()` — {{що відбувається в кожній ітерації}}.

Вивід у Serial Monitor (115200 бод):

```
{{приклад виводу}}
```

## Запуск

### На залізі (PlatformIO)

1. У [`platformio.ini`](../../platformio.ini) вкажи цей файл як джерело:
   ```ini
   build_src_filter = +<../{{ПАПКА ДЗ, напр. dz1.4 (LED + BUTTON)}}/{{варіант}}/main.cpp>
   ```
2. Збери, проший і відкрий монітор порту:
   ```sh
   pio run -t upload && pio device monitor
   ```

### У симуляторі (Wokwi for VS Code)

1. Збери прошивку: `pio run`.
2. `Wokwi: Select Config File` → вибери [`wokwi.toml`](wokwi.toml) з цієї папки.
3. Відкрий [`diagram.json`](diagram.json) і запусти `Wokwi: Start Simulator`.

## Файли

| Файл | Опис |
|---|---|
| [`main.cpp`](main.cpp) | прошивка |
| [`diagram.json`](diagram.json) | схема для Wokwi |
| [`wokwi.toml`](wokwi.toml) | конфіг симулятора (шлях до `firmware.bin` / `.elf`) |
| [`schema.png`](schema.png) | скриншот схеми |
| [`photo.jpeg`](photo.jpeg) | фото зібраної схеми |
| [`demo.gif`](demo.gif), [`video.mp4`](video.mp4), [`video.MOV`](video.MOV) | відео роботи |

<!-- Необов'язкові розділи: розкоментуй, якщо потрібно.

## Відомі проблеми

- {{опис проблеми}}: {{як відтворити / як виправити}}

-->

Інший варіант цього ДЗ: [**{{варіант}}**](../{{варіант}}/): {{короткий опис}}.
